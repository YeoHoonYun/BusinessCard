불변 클래스 : 값을 변경할 수 없는 클래스
String

assert

CoC(Convention over Configuration)
시스템들과 라이브러리, 프레임워크는 별도의 설정 시스템 없이 '바로 실행' 될 수 있어야 한다는 요구사항이 없더라도, 기본적으로 이러한 사항이 당연하다고 가정한다.

특정 문자열을 포함하는 지 체크
object.contains(String)

breakPoint

debug 하는 방법 스텝 오버 /